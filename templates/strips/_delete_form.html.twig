<form method="post" action="{{ path('app_strips_delete', {'id': strip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strip.id) }}">
    <button class="btn">Delete</button>
</form>
