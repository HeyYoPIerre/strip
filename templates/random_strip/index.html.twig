{% extends 'base.html.twig' %}

{% block title %}Strip{% endblock %}

{% block body %}
	<h1>Strip</h1>

	<div>{{ RandomStrip.name }}</div>
<div class="row">
<div class="col-6">
		<img src="{{ vich_uploader_asset(RandomStrip, 'imageFile') }}" alt="Strip" class="img-fluid mb-2 mb-md-0 me-md-3">
</div>
<div class="col-6">
		{{ RandomStrip.description }}
</div>
</div>

	<a href="{{ path('app_strips_index') }}">back to list</a>

	<a href="{{ path('app_strips_edit', {'id': RandomStrip.id}) }}">edit</a>

{% endblock %}
